'use client';
import React from 'react';

export default function LoginPage(){
  return (
    <div className="card">
      <h1 className="text-xl font-semibold mb-2">Entrar</h1>
      <p>En la integración real conectaremos con Supabase Auth (alias + contraseña).</p>
    </div>
  );
}
